"""
20 RAG pitfalls — one file per pit, bad vs. good side-by-side.

Each `pit_NN_*.py` is a standalone teaching artifact: a docstring describing the
symptom / root cause / solution, then an anti-pattern function and a
recommended replacement. Shared stub types live in `_common.py`.
"""
